<UserControl x:Class="cntrl.Controls.CustomField" x:Name="_CustomField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:cntrl.Controls"
             mc:Ignorable="d" 
            Height="80" Width="250" Loaded="CustomField_Loaded">
    <Grid>
        <StackPanel   HorizontalAlignment="Left" VerticalAlignment="Top">
            <Label Content="+"  PreviewMouseUp="Add_field" 
								   Style="{StaticResource btn_Label}"/>
            <ComboBox x:Name="cbxFieldType" Style="{StaticResource ComboToLabelStyle}" Width="250"
												  SelectedValuePath="id_field"
												  DisplayMemberPath="name" SelectionChanged="cbxFieldType_SelectionChanged"
												  SelectedValue="{Binding ElementName=_CustomField,Path=FieldID, Mode=TwoWay, NotifyOnValidationError=True, ValidatesOnExceptions=True, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}">
                <ComboBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel/>
                    </ItemsPanelTemplate>
                </ComboBox.ItemsPanel>
            </ComboBox>
            <TextBox Style="{StaticResource TextboxToLabelStyle}"
								 Text="{Binding ElementName=_CustomField,Path=FieldName, Mode=TwoWay, ValidatesOnDataErrors=true, UpdateSourceTrigger=PropertyChanged}" Width="250" />
        </StackPanel>
       
       
    </Grid>
</UserControl>
