<Page x:Class="Cognitivo.Commercial.PaymentType"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:entity="clr-namespace:entity;assembly=entity"
    xmlns:cntrl="clr-namespace:cntrl;assembly=cntrl"
    xmlns:local="clr-namespace:Cognitivo.Converters"
    xmlns:Panels="clr-namespace:cntrl.Panels;assembly=cntrl"
    mc:Ignorable="d"
    xmlns:lex="http://wpflocalizeextension.codeplex.com"
    lex:ResxLocalizationProvider.DefaultAssembly="Cognitivo"
    lex:ResxLocalizationProvider.DefaultDictionary="local"
    lex:LocalizeDictionary.DesignCulture="en-US"
    d:DesignHeight="450" d:DesignWidth="600"
	Title="Payment Type" Loaded="Page_Loaded"
    Background="{StaticResource appConfig}">

    <Page.Resources>
        <local:IsActive2ColorConverter x:Key="Converter" />
        <CollectionViewSource x:Key="payment_typeViewSource" d:DesignSource="{d:DesignInstance {x:Type entity:payment_type}, CreateList=True}" />
        <!--<CollectionViewSource x:Key="payment_typepayment_type_detailViewSource" Source="{Binding payment_type_detail, Source={StaticResource payment_typeViewSource}}" />
        <CollectionViewSource x:Key="payment_detail_viewsource" d:DesignSource="{d:DesignInstance {x:Type entity:payment_type}, CreateList=True}" />
        <CollectionViewSource x:Key="field_viewsource" d:DesignSource="{d:DesignInstance {x:Type entity:payment_type}, CreateList=True}" />-->
    </Page.Resources>

    <Grid DataContext="{StaticResource payment_typeViewSource}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="3" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <StackPanel>
            <StackPanel.Background>
                <SolidColorBrush Color="Black" Opacity="0.05" />
            </StackPanel.Background>
            <Image Style="{StaticResource img_Config}"
                   Source="/Cognitivo;component/Images/Application/128/Money.png" />

            <Label Content="{lex:Loc PaymentType}" HorizontalAlignment="Center"
                   Style="{StaticResource H1}" />
            <Button x:Name="btnNew" Content="{lex:Loc Create}" Margin="15"
                        Style="{StaticResource btn_green}" Click="btnNew_Click" />
            <TextBlock Text="Types of payment"
                            HorizontalAlignment="Center"
                            Style="{StaticResource text_Instructions}" />
        </StackPanel>

        <Rectangle Grid.Column="1" Fill="Silver" Width="1" Margin="0,50" />
        <ItemsControl Name="itemsource" ItemsSource="{Binding}" Grid.Column="2">
            <!-- host the items generated by this ItemsControl in a grid -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel ScrollViewer.VerticalScrollBarVisibility="Auto" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <!-- render each bound item using a TextBlock-->
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Panels:pnl_Curd Margin="5"
                                            Status="{Binding is_active, Converter={StaticResource Converter}}"
                                            DisplayName="{Binding name}" Id="{Binding id_payment_type}"
                                            linkEdit_click="pnl_Curd_linkEdit_click" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Grid Name="crud_modal" Background="#7FC0C0C0" Visibility="Hidden" Grid.ColumnSpan="3" />
    </Grid>
</Page>