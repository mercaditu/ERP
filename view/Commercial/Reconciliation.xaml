<Page x:Class="Cognitivo.Commercial.Reconciliation"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
	  Title="Reconciliation">
    <Grid Background="{StaticResource winBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="1" Grid.Column="1" Margin="0,50" Background="Azure">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Label Content="Bank" Style="{StaticResource H2}"/>
                <Button Content="Import" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="8"/>
                <DataGrid Margin="0,50,0,0"/>
            </Grid>
            <Grid Grid.Column="2">
                <Label Content="System" Style="{StaticResource H2}"/>
                <DataGrid Margin="0,50,0,0"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
