<Page x:Class="Cognitivo.Product.InventoryFlow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cognitivo.Product"
      xmlns:cntrl="clr-namespace:cntrl;assembly=cntrl"
      	xmlns:lex="http://wpflocalizeextension.codeplex.com"
		lex:ResxLocalizationProvider.DefaultAssembly="Cognitivo"
		lex:ResxLocalizationProvider.DefaultDictionary="local"
		lex:LocalizeDictionary.DesignCulture="en-US"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="800"
      Title="InventoryFlow">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Label Grid.Column="1" HorizontalAlignment="Right" Content="{lex:Loc InventoryFlow}" Style="{StaticResource H3}"/>
        <Grid Grid.Column="0">
            <TextBox x:Name="txtsearch" Height="26" Margin="7,0" VerticalAlignment="Center" Padding="14,0,0,0" TextChanged="TextBox_TextChanged" />
            <Label Content="L" Style="{StaticResource ico_Label}" HorizontalAlignment="Left" Margin="7,0" Foreground="Silver" />
        </Grid>
        <DataGrid Grid.Row="1" Grid.Column="0" FontSize="10" Margin="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" />
                <DataGridTextColumn Header="Item" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Background="{StaticResource appConfig}">
            
        </StackPanel>
    </Grid>
</Page>
