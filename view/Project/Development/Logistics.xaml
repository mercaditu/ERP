<Page x:Class="Cognitivo.Project.Development.Logistics" x:Name="Logistic"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:metro="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
      mc:Ignorable="d" 
      xmlns:Controls="clr-namespace:cntrl.Controls;assembly=cntrl"  
      xmlns:entity="clr-namespace:entity;assembly=entity"
      xmlns:lex="http://wpflocalizeextension.codeplex.com" 
      xmlns:cntrl="clr-namespace:cntrl;assembly=cntrl"
      lex:ResxLocalizationProvider.DefaultAssembly="Cognitivo" 
      lex:ResxLocalizationProvider.DefaultDictionary="local"
      lex:LocalizeDictionary.DesignCulture="en-US" 
      MinHeight="500" MinWidth="800" Loaded="Page_Loaded"
	Title="Logistics">
    <Page.Resources>
        <CollectionViewSource x:Key="projectViewSource" d:DesignSource="{d:DesignInstance {x:Type entity:project}, CreateList=True}"/>
        
        <CollectionViewSource x:Key="conditionViewSource" d:DesignSource="{d:DesignInstance {x:Type entity:app_condition}, CreateList=True}"/>
        <CollectionViewSource x:Key="contractViewSource" d:DesignSource="{d:DesignInstance {x:Type entity:app_contract}, CreateList=True}"/>
        <CollectionViewSource x:Key="app_dimensionViewSource" d:DesignSource="{d:DesignInstance {x:Type entity:app_dimension}, CreateList=True}"/>
        <CollectionViewSource x:Key="item_dimensionViewSource" d:DesignSource="{d:DesignInstance {x:Type entity:item_dimension}, CreateList=True}"/>
        <CollectionViewSource x:Key="app_measurementViewSource" d:DesignSource="{d:DesignInstance {x:Type entity:app_measurement}, CreateList=True}"/>
        <CollectionViewSource x:Key="project_task_dimensionViewSource" d:DesignSource="{d:DesignInstance {x:Type entity:project_task_dimension}, CreateList=True}"/>
    </Page.Resources>
    <Grid DataContext="{StaticResource projectViewSource}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--<Grid Name="crud_modal" Background="{StaticResource Modal_BackgroundColor}" Visibility="Collapsed"
              Grid.ColumnSpan="2" Panel.ZIndex="5">
            <Border>
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="8" Opacity="0.75"/>
                </Border.Effect>
                <StackPanel Background="White" x:Name="stackFields" Width="360" Height="432">
                    <Grid>
                        <Border Background="WhiteSmoke" />
                        <Border Width="45" HorizontalAlignment="Left" Background="#66ccff">
                            <Image Source="/cntrl;component/Images/Outline_White/price_tag_USD_filled-100.png" 
                                   Height="32" Opacity="0.64"/>
                        </Border>
                        <Label x:Name="lblTitle" Content="{lex:Loc Purchase Tender}" FontSize="25" Margin="50,0,0,0"/>
                        <Label x:Name="lblCancel" Content="c" FontSize="18" VerticalAlignment="Center" Margin="5"
                               HorizontalAlignment="Right" MouseDown="lblCancel_MouseDown" FontFamily="/cntrl;component/Resources/#cognitivo" Cursor="Hand"/>
                    </Grid>
                    <TextBlock Text="Create purchase tender" Style="{StaticResource text_Instructions}"/>
                    <StackPanel HorizontalAlignment="Center">
                        <Label Content="{lex:Loc Name}" Style="{StaticResource input_label}"/>
                        <TextBox x:Name="txtName" Style="{StaticResource input_textbox}"/>
                        <Label Content="{lex:Loc Condition}"  Style="{StaticResource input_label}"/>
                        <ComboBox x:Name="cbxCondition" Style="{DynamicResource input_combobox}" DisplayMemberPath="name"
                                  metro:ControlsHelper.FocusBorderBrush="RoyalBlue"
                                  metro:ControlsHelper.MouseOverBorderBrush="Silver"
                                  ItemsSource="{Binding Source={StaticResource conditionViewSource}}"
					              SelectedValuePath="id_condition" SelectionChanged="cbxCondition_SelectionChanged"/>
                        <Label Content="{lex:Loc Contract}"  Style="{StaticResource input_label}"/>
                        <ComboBox Style="{DynamicResource input_combobox}" DisplayMemberPath="name" 
                                  metro:ControlsHelper.FocusBorderBrush="RoyalBlue"
                                  metro:ControlsHelper.MouseOverBorderBrush="Silver"
                                  SelectedValuePath="id_contract" x:Name="cbxContract" 
                                  ItemsSource="{Binding Source={StaticResource contractViewSource}}"/>
                        <Label Content="{lex:Loc Currency}"  Style="{StaticResource input_label}"/>
                        <Controls:CurrencyBox x:Name="cbxCurrency"/>
                        <Label Content="{lex:Loc Comment}" Style="{StaticResource input_label}"/>
                        <TextBox x:Name="txtComment" Style="{StaticResource input_textbox}"/>
                    </StackPanel>
                    <Grid Background="WhiteSmoke" Margin="0,10,2,0">
                        <Button x:Name="btnSaveTender" Content="{lex:Loc Save}" Foreground="RoyalBlue" HorizontalAlignment="Right"
                                FontWeight="Medium" BorderThickness="0" Cursor="Hand" Focusable="True" Click="btnSaveTender_Click"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>-->
        <Grid Name="crud_modal" Background="{StaticResource Modal_BackgroundColor}" Visibility="Collapsed"
              Grid.RowSpan="2" Grid.ColumnSpan="2" Panel.ZIndex="5"/>

            <ListBox Background="{DynamicResource AccentColor_Brush}" x:Name="projectDataGrid" Grid.Row="1"
			     ScrollViewer.VerticalScrollBarVisibility="Auto"
			     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
			     ItemContainerStyle="{StaticResource nav_ListSelection}" Style="{StaticResource nav_List}"
			     ItemsSource="{Binding}" SelectionChanged="projectDataGrid_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <cntrl:navList recordCode="{Binding est_start_date, StringFormat=d}" 
						           recordSecondaryName="{Binding contact.name,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
						           recordName="{Binding name}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="1" Grid.Column="1" Background="{StaticResource winBackground}">
            <StackPanel Grid.Row="0">
                <TabControl HorizontalAlignment="Stretch" metro:TabControlHelper.IsUnderlined="True" x:Name="TabLogistics">
                    <TabItem Header="{lex:Loc Product}" x:Name="tabproduct">
                        <DataGrid x:Name="item_ProductDataGrid" 
                                  VerticalScrollBarVisibility="Auto"
                                  AutoGenerateColumns="False" Grid.Column="1"
                                  EnableRowVirtualization="True"  Grid.Row="1" IsReadOnly="True" 
                                  SelectionChanged="item_ProductDataGrid_SelectionChanged" Margin="0,0,300,0" 
                                  LoadingRowDetails="item_ProductDataGrid_LoadingRowDetails" >
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding _code}" Header="Code" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding _name}" Header="Product" Width="*" />
                                <DataGridTextColumn Binding="{Binding _ordered_quantity}" Header="Qty Needed" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding avlqtyColumn}" Header="Qty Available" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding buyqty}" Header="buy qty" Width="SizeToHeader" />
                            </DataGrid.Columns>
                            <DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <DataGrid x:Name="item_movementDataGrid" 
                  VerticalScrollBarVisibility="Auto"
                  AutoGenerateColumns="False" Grid.Column="1"
                  EnableRowVirtualization="True"  Grid.Row="1" 
                  IsReadOnly="True">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Binding="{Binding code}" Header="Code" Width="*" />
                                            <DataGridTextColumn Binding="{Binding name}" Header="Product" Width="*" />
                                            <DataGridTextColumn Binding="{Binding BranchName}" Header="BranchName" Width="*" />
                                            <DataGridTextColumn Binding="{Binding quntitiy}" Header="Quantity" Width="*" />
                                            <DataGridTextColumn Binding="{Binding measurement}" Header="Measurement" Width="*" />
                                        </DataGrid.Columns>

                                    </DataGrid>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="{lex:Loc RawMaterial}" x:Name="tabraw" HorizontalAlignment="Left" Width="164">
                        <DataGrid x:Name="item_RawDataGrid" 
                  VerticalScrollBarVisibility="Auto"
                  AutoGenerateColumns="False" Grid.Column="1"
                  EnableRowVirtualization="True"  Grid.Row="1" SelectionChanged="item_RawDataGrid_SelectionChanged" LoadingRowDetails="item_ProductDataGrid_LoadingRowDetails"
                  IsReadOnly="True" Margin="0,0,300,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding _code}" Header="Code" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding _name}" Header="Product" Width="*" />
                                <DataGridTextColumn Binding="{Binding _ordered_quantity}" Header="Qty Needed" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding avlqtyColumn}" Header="Qty Available" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding buyqty}" Header="buy qty" Width="SizeToHeader" />
                            </DataGrid.Columns>
                            <DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <DataGrid x:Name="item_movementDataGrid" 
                  VerticalScrollBarVisibility="Auto"
                  AutoGenerateColumns="False" Grid.Column="1"
                  EnableRowVirtualization="True"  Grid.Row="1" 
                  IsReadOnly="True">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Binding="{Binding code}" Header="Code" Width="*" />
                                            <DataGridTextColumn Binding="{Binding name}" Header="Product" Width="*" />
                                            <DataGridTextColumn Binding="{Binding BranchName}" Header="BranchName" Width="*" />
                                            <DataGridTextColumn Binding="{Binding quntitiy}" Header="Quantity" Width="*" />
                                            <DataGridTextColumn Binding="{Binding measurement}" Header="Measurement" Width="*" />
                                        </DataGrid.Columns>

                                    </DataGrid>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>
                        </DataGrid>
                    </TabItem>
                    <TabItem Header="{lex:Loc Service}" x:Name="tabservice">
                        <DataGrid x:Name="item_ServiceDataGrid" 
                  VerticalScrollBarVisibility="Auto"
                  AutoGenerateColumns="False" Grid.Column="1"
                  EnableRowVirtualization="True"  Grid.Row="1" SelectionChanged="item_ServiceDataGrid_SelectionChanged"
                  RowDetailsVisibilityMode="VisibleWhenSelected"  IsReadOnly="True" Margin="0,0,300,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding _code}" Header="Code" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding _name}" Header="Product" Width="*" />
                                <DataGridTextColumn Binding="{Binding _ordered_quantity}" Header="Qty Needed" Width="SizeToHeader" />
                                <!--<DataGridTextColumn Binding="{Binding _avlqtyColumn}" Header="Qty Available" Width="SizeToHeader" />-->
                            </DataGrid.Columns>

                        </DataGrid>
                    </TabItem>
                    <TabItem Header="{lex:Loc FixedAssets}" x:Name="tabasset">
                        <DataGrid x:Name="item_CapitalDataGrid" 
                                  VerticalScrollBarVisibility="Auto"
                                  AutoGenerateColumns="False" Grid.Column="1"
                                  EnableRowVirtualization="True"  Grid.Row="1" SelectionChanged="item_CapitalDataGrid_SelectionChanged" LoadingRowDetails="item_ProductDataGrid_LoadingRowDetails"
                                  RowDetailsVisibilityMode="VisibleWhenSelected"  IsReadOnly="True" Margin="0,0,300,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding _code}" Header="Code" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding _name}" Header="Product" Width="*" />
                                <DataGridTextColumn Binding="{Binding _ordered_quantity}" Header="Qty Needed" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding avlqtyColumn}" Header="Qty Available" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding buyqty}" Header="buy qty" Width="SizeToHeader" />
                            </DataGrid.Columns>
                            <!--<DataGrid.RowDetailsTemplate>
                                <DataTemplate>
                                    <DataGrid x:Name="project_task_dimensionDataGrid" AutoGenerateColumns="False" EnableRowVirtualization="True" 
							                       ItemsSource="{Binding Source={StaticResource project_task_dimensionViewSource}}" 
							                       RowDetailsVisibilityMode="VisibleWhenSelected">
                                        <DataGrid.Columns>
                                            <DataGridComboBoxColumn x:Name="dimentionComboCol" Header="Dimension" 
									                        ItemsSource="{Binding Source={StaticResource app_dimensionViewSource}}" DisplayMemberPath="name" SelectedValuePath="id_app_dimension"
									                        SelectedValueBinding="{Binding id_app_dimension, UpdateSourceTrigger=PropertyChanged}"/>
                                            <DataGridTextColumn x:Name="valueColumn2" Binding="{Binding value, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Header="value" />
                                            <DataGridComboBoxColumn x:Name="AppMeasurementComboBox" 
									                        DisplayMemberPath="name" SelectedValuePath="id_measurement"  
									                        ItemsSource="{Binding Source={StaticResource app_measurementViewSource}}"
									                        SelectedValueBinding="{Binding id_measurement, UpdateSourceTrigger=PropertyChanged}" Header="Measurement" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </DataTemplate>
                            </DataGrid.RowDetailsTemplate>-->
                        </DataGrid>
                    </TabItem>

                    <TabItem Header="{lex:Loc Supplies}" x:Name="tabSupplies">
                        <DataGrid x:Name="dgvSupplies" 
                                  VerticalScrollBarVisibility="Auto"
                                  AutoGenerateColumns="False" Grid.Column="1"
                                  EnableRowVirtualization="True"  Grid.Row="1" SelectionChanged="dgvSupplies_SelectionChanged"
                                  RowDetailsVisibilityMode="VisibleWhenSelected"  IsReadOnly="True" Margin="0,0,300,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding _code}" Header="Code" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding _name}" Header="Product" Width="*" />
                                <DataGridTextColumn Binding="{Binding _ordered_quantity}" Header="Qty Needed" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding avlqtyColumn}" Header="Qty Available" Width="SizeToHeader" />
                                <DataGridTextColumn Binding="{Binding buyqty}" Header="buy qty" Width="SizeToHeader" />
                            </DataGrid.Columns>

                        </DataGrid>
                    </TabItem>

                </TabControl>

            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="2" Margin="0,42,0,0" Background="WhiteSmoke" HorizontalAlignment="Right">
            <StackPanel  Grid.Column="3" >
                <Button Content="{lex:Loc ItemRequest}" Style="{StaticResource btn_green}" 
                   HorizontalAlignment="Center" 
                    Margin="0,7,7,0" VerticalAlignment="Top" Click="Button_Click" />
                <StackPanel Orientation="Horizontal">
                    <Label Content="Selected Rows:-"/>
                    <Label Content="{Binding noofrows,ElementName=Logistic,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <DataGrid x:Name="itemDataGrid" AutoGenerateColumns="False" EnableRowVirtualization="True" Grid.Row="0"
                      CanUserAddRows="False" RowDetailsVisibilityMode="VisibleWhenSelected" HorizontalAlignment="Right" CellEditEnding="itemDataGrid_CellEditEnding"
                      SelectionChanged="itemDataGrid_SelectionChanged" LoadingRowDetails="itemDataGrid_LoadingRowDetails"
                      Width="300" >
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Binding="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="SizeToHeader" />
                        <DataGridTextColumn Binding="{Binding parent.code}" Header="Code" Width="SizeToHeader" IsReadOnly="True" />
                        <DataGridTextColumn Binding="{Binding parent.item_description}" Header="Task" Width="*" IsReadOnly="True" />
                        <DataGridTextColumn Binding="{Binding quantity_est, StringFormat=N2}" Header="QtyNeed" Width="SizeToHeader" IsReadOnly="True" />
                    </DataGrid.Columns>
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <DataGrid x:Name="project_task_dimensionDataGrid" AutoGenerateColumns="False" EnableRowVirtualization="True" 
							        ItemsSource="{Binding Source={StaticResource project_task_dimensionViewSource}}" 
							        RowDetailsVisibilityMode="VisibleWhenSelected">
                                <DataGrid.Columns>
                                    <DataGridComboBoxColumn x:Name="dimentionComboCol" Header="Dimension" 
									                        ItemsSource="{Binding Source={StaticResource app_dimensionViewSource}}" DisplayMemberPath="name" SelectedValuePath="id_dimension"
									                        SelectedValueBinding="{Binding id_dimension, UpdateSourceTrigger=PropertyChanged}"/>
                                    <DataGridTextColumn x:Name="valueColumn2" Binding="{Binding value, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" Header="value" />
                                    <DataGridComboBoxColumn x:Name="AppMeasurementComboBox" 
									                        DisplayMemberPath="name" SelectedValuePath="id_measurement"  
									                        ItemsSource="{Binding Source={StaticResource app_measurementViewSource}}"
									                        SelectedValueBinding="{Binding id_measurement, UpdateSourceTrigger=PropertyChanged}" Header="Measurement" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>
            </StackPanel>
        </Grid>

        <cntrl:toolBar Grid.ColumnSpan="2" x:Name="toolBar" appName="ProjectLogistics"
                       btnSearch_Click="toolBar_btnSearch_Click"/>
    </Grid>
</Page>
