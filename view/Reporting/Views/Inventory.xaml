<Page x:Class="Cognitivo.Reporting.Views.Inventory" x:Name="_Inventory"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      xmlns:rv="clr-namespace:Microsoft.Reporting.WinForms;assembly=Microsoft.ReportViewer.WinForms"
	Title="Contacts">
    <Grid Background="Gainsboro">
        <Grid VerticalAlignment="Top" Height="58" Margin="16,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox Content="Customer" IsChecked="{Binding ElementName=_Inventory, Path=IsCustomer, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="128" Style="{StaticResource input_checkbox}"/>
                <CheckBox Content="Supplier" IsChecked="{Binding ElementName=_Inventory, Path=IsSupplier, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Width="128" Style="{StaticResource input_checkbox}"/>
                <Button Content="Filter" VerticalAlignment="Center" Click="Fill" Width="64"/>
            </StackPanel>
        </Grid>
        <WindowsFormsHost HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,58,0,0">
            <rv:ReportViewer x:Name="reportViewer"/>
        </WindowsFormsHost>
    </Grid>
</Page>
