<Page x:Class="Cognitivo.Reporting.Views.PurchaseOrder" x:Name="this"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	  xmlns:Cntrl="clr-namespace:cntrl;assembly=cntrl"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	  xmlns:rv="clr-namespace:Microsoft.Reporting.WinForms;assembly=Microsoft.ReportViewer.WinForms"
	  mc:Ignorable="d" 
	  d:DesignHeight="600" d:DesignWidth="800"
	Title="PurchaseOrder">
	<Page.Resources>
		<BooleanToVisibilityConverter x:Key="Bool2Visibility"/>
	</Page.Resources>
	<Grid Background="{StaticResource {x:Static SystemColors.AppWorkspaceBrushKey}}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*"/>
			<ColumnDefinition Width="3*"/>
		</Grid.ColumnDefinitions>
		<Cntrl:ReportPanel 
			x:Name="ReportPanel" CalendarSelectionMode="SingleRange"
			ReportTitle="{Binding ElementName=this, Path=Title, Mode=OneTime, UpdateSourceTrigger=PropertyChanged}"
			ReportDescription="Get List of Purchase per Brand. Helps calculate which brand offer the best returns."
			Update="Fill"
            Filter="Filter"
			Grid.Column="0"/>
		<WindowsFormsHost HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="1">
			<rv:ReportViewer x:Name="reportViewer"/>
		</WindowsFormsHost>
	</Grid>
</Page>
