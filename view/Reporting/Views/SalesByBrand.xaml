<Page x:Class="Cognitivo.Reporting.Views.SalesByBrand" x:Name="_SalesByBrand"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      xmlns:Cntrl="clr-namespace:cntrl;assembly=cntrl"
      xmlns:rv="clr-namespace:Microsoft.Reporting.WinForms;assembly=Microsoft.ReportViewer.WinForms"
      d:DesignHeight="600" d:DesignWidth="800"
	Title="Sales By Brand">

    <Grid Background="Gainsboro">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Cntrl:ReportPanel 
            x:Name="ReportPanel"
            ReportTitle="{Binding ElementName=_SalesByBrand, Path=Title, Mode=OneTime, UpdateSourceTrigger=PropertyChanged}"
            ReportDescription="Get List of Sales per Brand. Helps calculate which brand offer the best returns."
            ShowBranch="True"
            Grid.Column="0"/>
        <WindowsFormsHost Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <rv:ReportViewer x:Name="reportViewer"/>
        </WindowsFormsHost>
    </Grid>
</Page>
